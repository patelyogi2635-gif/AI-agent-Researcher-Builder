# ── LangGraph core ────────────────────────────────────────────────
langgraph>=0.2.28
langchain>=0.2.16
langchain-core>=0.2.38
langchain-groq>=0.1.9          # Free LLM: llama-3.3-70b

# ── Phase 2 deps (install now, use later) ─────────────────────────
langchain-community>=0.2.16    # DuckDuckGo, BS4 wrappers
tavily-python>=0.3.3           # Free: 1000 searches/month
beautifulsoup4>=4.12.3
requests>=2.32.3
chromadb>=0.5.5                # Local vector store — fully free

# ── Config & validation ───────────────────────────────────────────
pydantic>=2.8.2
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# ── Logging & CLI ─────────────────────────────────────────────────
rich>=13.7.1                   # Beautiful terminal output
typer>=0.12.5                  # CLI framework

# ── Phase 3 deps (install now, use later) ─────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
streamlit>=1.37.0

gunicorn>=21.2.0